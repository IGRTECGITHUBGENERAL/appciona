import 'package:flutter/material.dart';

class MediaPageYotube extends StatelessWidget {
  const MediaPageYotube({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
